---
title: Overview
description: Embed Arcube's ancillary booking widget directly into your airline's booking flow.
---

This guide explains how to integrate the Arcube booking widget into an airline booking platform. The widget is an embedded ancillary experience that appears inside the airline booking flow. It helps passengers discover and select additional services such as airport transfers, car rental, lounge access, eSIMs, carbon offset, insurance, and other ancillary products.

The widget does not replace the airline booking engine. The airline still owns:

- the booking session
- the flight selection
- the basket
- the checkout
- the final order

The widget adds a passenger-facing ancillary layer on top of the airline booking journey.

---

## End-to-End Flow

The diagram below shows the full integration at a glance - from the moment the passenger lands on the airline website through search, trip selection, passenger details, widget rendering, ancillary selection, and the final payment handled by the airline.

<img src="/images/widget/widget-general-flow.png" alt="End-to-end widget integration flow" style={{display: 'block', margin: '0 auto', width: '100%'}} />

<small style={{display: 'block', textAlign: 'center'}}>End-to-end widget integration flow.</small>

A few things to notice:

- The **airline IBE** owns the booking session, the search, the trip selection, and the passenger details.
- The **Airline CDN/EDGE** is the secure layer that requests a widget token on behalf of the browser, so the airline secret never reaches the passenger's device.
- The **Arcube Backend** issues the short-lived token and powers the widget's ancillary catalog and offers.
- Once the widget is rendered with a valid token and the PNR context, all ancillary interactions (view extras, add eSIM, etc.) happen inside the widget and are emitted back to the airline page as cart events.
- Payment and final order processing remain entirely on the airline side.

---

## How It Works

In most integrations, the widget is shown on the extras or ancillary step. A common pattern includes:

- Loading the Arcube integration script on the booking site
- Waiting until the correct booking step and booking data are available
- Sending the booking context to Arcube
- Opening the widget inside the airline page
- Fetching offers and showing them to the passenger
- Letting the passenger select services
- Handing selected items back to the airline basket flow

### Operating Model

- Prepare recommendations as soon as there is a stable booking session
- Render the widget on the extras step where the passenger is expected to buy ancillaries

---

## Customization

The widget can be fully branded to match your airline experience using a hosted theme JSON file. Supported customizations include:

- Colors (primary, secondary, background, neutral, and more)
- Typography and fonts
- Logo

---

## Benefits

- Seamless user experience embedded in the booking flow
- Real-time personalization based on booking context
- Fully branded to match your airline's design

---

## Payment Flow

You process payments; Arcube invoices for ancillaries.

---

## Security

- The airline secret never reaches the browser - only a short-lived token is passed to the widget
- HMAC-signed requests for token vending
- HTTPS encryption for all API and widget communication
